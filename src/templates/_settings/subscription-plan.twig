{% extends pluginHandle ~ '/_settings/_layout.twig' %}

{# Placeholder: this page will link to the Craft Plugin Store so the user can manage the
   plugin's edition. #}

{% block content %}
    <p class="light">{{ 'Subscription Plan'|t(pluginHandle) }}</p>
{% endblock %}

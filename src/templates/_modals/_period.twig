{% import "_includes/forms" as forms %}

{{ forms.selectField({
    label: 'Verify for'|t(pluginHandle),
    id: 'verificationPeriod',
    name: 'verificationPeriod',
    options: periodSelectOptions,
    toggle: true
}) }}

<div id="specific-date" class="hidden">
    {{ forms.dateField({
        label: 'Date'|t('app'),
        id: 'specificDate',
        name: 'specificDate',
        value: now,
    }) }}
</div>
